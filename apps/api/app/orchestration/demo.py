"""Removed in the persistence refactor (v0.3). Superseded by:
- app.orchestration.workflow (MissionOrchestrator)
- app.orchestration.coordinator (SSE + resume)
- app.services.mission_service / app.services.approval_service
This module intentionally left as a no-op shim to avoid stale imports.
"""
